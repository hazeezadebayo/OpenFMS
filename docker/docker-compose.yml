services:
  mqtt:
    image: eclipse-mosquitto
    container_name: openfms-mqtt
    pull_policy: if_not_present # never reach out to Docker Hub if image is already local
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ../config/mosquitto.conf:/mosquitto/config/mosquitto.conf
    restart: unless-stopped

  db:
    image: postgres:13
    container_name: openfms-db
    pull_policy: if_not_present # never reach out to Docker Hub if image is already local
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    restart: unless-stopped

  manager:
    image: openfms:latest
    container_name: openfms-manager
    build:
      context: ..
      dockerfile: docker/Dockerfile
    pull_policy: if_not_present # only pull base image during --build if not already cached
    depends_on:
      - mqtt
      - db
    environment:
      MQTT_BROKER: mqtt
      MQTT_PORT: 1883
      POSTGRES_HOST: db
      PYTHONUNBUFFERED: 1
    volumes:
      - ../config:/app/config
      - ../logs:/app/logs
    stdin_open: true # docker run -i
    tty: true # docker run -t
    command: python3 fleet_management/FmMain.py

  simulator:
    image: openfms:latest
    container_name: openfms-simulator
    build:
      context: ..
      dockerfile: docker/Dockerfile
    pull_policy: if_not_present
    depends_on:
      - mqtt
      - db
    environment:
      MQTT_BROKER: mqtt
      MQTT_PORT: 1883
      POSTGRES_HOST: db
      PYTHONUNBUFFERED: 1
    volumes:
      - ../config:/app/config
      - ../logs:/app/logs
    command: python3 fleet_management/FmRobotSimulator.py

  # Optional: Interface wrapper to run automated scenarios
  scenario:
    image: openfms:latest
    container_name: openfms-scenario
    build:
      context: ..
      dockerfile: docker/Dockerfile
    pull_policy: if_not_present
    depends_on:
      - mqtt
      - db
    environment:
      MQTT_BROKER: mqtt
      MQTT_PORT: 1883
      POSTGRES_HOST: db
      PYTHONUNBUFFERED: 1
      SCENARIO_NAME: ${SCENARIO_NAME:-S1}
    volumes:
      - ../config:/app/config
      - ../logs:/app/logs
    # Wait for manager to be ready and run scenario
    command: /bin/bash -c "sleep 10 && python3 fleet_management/FmInterface.py run $${SCENARIO_NAME:-S1}"

  # Optional: Dashboard viewer
  dashboard:
    image: openfms:latest
    container_name: openfms-dashboard
    build:
      context: ..
      dockerfile: docker/Dockerfile
    pull_policy: if_not_present
    depends_on:
      - scenario
    environment:
      PYTHONUNBUFFERED: 1
    volumes:
      - ../logs:/app/logs
    tty: true
    stdin_open: true
    command: /bin/sh -c "while true; do clear; if [ -f logs/live_dashboard.txt ]; then cat logs/live_dashboard.txt; else echo 'Waiting for Fleet Manager to produce dashboard...'; fi; sleep 0.5; done"
